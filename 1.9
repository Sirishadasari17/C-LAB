#include<stdio.h>
int main()
{
	int p,t,r;
	float si;
	printf("Enter p value:");
	scanf("%d",&p);
	printf("Enter t value:");
	scanf("%d",&t);
	printf("Enter r value:");
	scanf("%d",&r);
	si=(p*t*r)/100.0;
	printf("\n SI is %f",si);
	return 0;
}

