#include<stdio.h>
int main()
{
	int num;
	printf("Enter a number:");
	scanf("%d",&num);
	if(sum=num)
	{
		printf("%d is an Armstrong number\n",num);
	}
	else 
	{
	    printf("%d is not an Armstrong number\n",num);	
	}
	return 0;
}
